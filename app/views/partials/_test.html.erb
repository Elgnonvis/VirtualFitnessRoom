<header class="p-2 bg-primary text-white fixed-top">
    <div class="container">
        <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
            <a href="/" class="navbar-brand d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none"><span class="fb">VirtualFitnessRoom </span>
            </a>
            
            </a>
            <ul class="nav me-lg-auto mb-2 justify-content-center mb-md-0"></ul>
            <% if user_signed_in? %> 
                <div class="text-end me-4">
                </div>
                <div class="text-end me-4">
                </div>
                <div class="text-end me-4">
                    <a href="<%= new_user_invitation_path %>" class="btn btn-success" role="button">Invitation</a>
                </div>

                <div class="dropdown text-end">
                    <a href="#" class="d-block link-dark text-decoration-none dropdown-toggle" id="dropdownUser1" data-bs-toggle="dropdown" aria-expanded="false">
                        <% if current_user.avatar? %>
                            <%= image_tag current_user.avatar.url, class:"rounded-circle", height:32, width:32, alt:"Avatar" %>
                        <% else %>
                            <img src="/avatar.png" class="rounded-circle",height="32", width="32", alt="Avatar" >
                        <% end %>
                    </a>
                    <ul class="dropdown-menu text-small text-decoration-none w-100" aria-labelledby="dropdownUser1">
                        <%= link_to "Mon profile", profil_path, class:"btn btn-secondary ms-2 w-100" %>

                        <li><hr class="dropdown-divider"></li>
                        <%= link_to "Se déconnecter", destroy_user_session_path, method: :delete, class:"btn btn-danger ms-2" %>
                    </ul>
                </div>

            <% else %> 
                <div class="text-end text-decoration-none">
                    <a class="btn btn-success me-2" href="<%= new_user_session_path %>" role="button">Se connecter</a>
                    <a class="btn btn-warning" href="<%= new_user_registration_path %>" role="button">S'inscrire</a>
                </div> 
            <% end %>
        </div>
    </div>
</header>