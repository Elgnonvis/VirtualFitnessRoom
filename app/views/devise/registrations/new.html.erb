<title> VirtualFitnessRoom | Inscription</title><br>
<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), wrapper: :input_group) do |f| %>
<%= f.error_notification %>
<div class=" row container-fluid mt-4" >
  <h2 class=" text-center pb-3"><%= t(".sign_up") %></h2>
  <div class="col d-none d-md-block" style = "overflow: hidden; padding-left: 0 !important;" >
    <img style = "height: 628px; " src="/accueil1.jpeg" alt="">
  </div>

  <div class=" col-md-6 form-inputs" style = "padding-top: 1rem; border: 1px solid;">
    <div class=" w-100 d-flex flex-column container justify-content-center align-itmes-center mb-1" >
      <%= f.input :gender, prepend: "❓", required: true, :collection => ["Masculin", "Féminin"],:input_html => { :class => 'form-control'}, prompt: "Choose From List" %> <br />
      <%= f.input :username,
                    prepend: "@",
                    required: true,
                    placeholder: "Surnom ou nom d'utilisateur",
                    input_html: { autocomplete: "username" }%> <br>
      <%= f.input :email,
                    prepend: "✉",
                    placeholder: 'Un mail valide',
                    required: true,
                    input_html: { autocomplete: "email" }%> <br>
      <%= f.input :password,
                      prepend: "🗝",
                      placeholder: 'Mot de passe',
                      required: true,
                      hint: (t('devise.shared.minimum_password_length', count: @minimum_password_length) if @minimum_password_length),
                      input_html: { autocomplete: "new-password" } %> <br>
      <%= f.input :password_confirmation,
                      prepend: "🔑",
                      placeholder: 'Re-entrer le mot de passe ci-dessus',
                      required: true,
                      input_html: { autocomplete: "new-password" } %>
      <div class="form-actions">
        <%= f.button :submit, t(".sign_up"), class:"btn btn-success mt-3 w-100" %>
      </div>
        <% end %>
        <%= render "devise/shared/links" %>
    </div>
    </div>
  </div>
</div>